version: "3.7"

services:
  app_proxy:
    image: getumbrel/app-proxy:1.7.0
    container_name: giveaways_alert_proxy
    environment:
      APP_HOST: giveaways_alert_server
      APP_PORT: 4444
    networks:
      - giveaways_alert_network
    depends_on:
      - server

  server:
    image: "jermorg/giveaways-alert@sha256:9e524e56883c3e7c4300c32c7516e3ad3fd2e91ba4497df645ac3fdde07a9309"
    container_name: giveaways_alert_server
    restart: always
    environment:
      - BotToken=TelegramBotToken 
      - Channel=@channel
      - PORT=4444
      - Url=https://www.gamerpower.com/api/filter?platform=epic-games-store.steam&type=game.loot
      - Time=300
    volumes:
      - "${APP_DATA_DIR}/data:/app/data"
    networks:
      - giveaways_alert_network

networks:
  giveaways_alert_network:
    driver: bridge